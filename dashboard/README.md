# Dashboard

This work lives in [`../webapp/apps/dashboard/`](../webapp/apps/dashboard/) — it's part of a
self-contained Django application (dashboard + chatbot + supporting pages) rather than a
standalone module, so it's kept together with the rest of that app instead of split out here.

See [`../webapp/README.md`](../webapp/README.md) for the full write-up: the KPIs/charts, the
Year/Country filters, and the Gold-layer data (`apps/gold_data/`) they're served from.
